# OpsFlow AI — example environment file (synthetic project, no real credentials).
# Copy to .env and adjust for local development. These defaults match docker-compose.yml.
OPSFLOW_DB_HOST=localhost
OPSFLOW_DB_PORT=5432
OPSFLOW_DB_NAME=opsflow
OPSFLOW_DB_USER=opsflow
OPSFLOW_DB_PASSWORD=opsflow_local_dev
